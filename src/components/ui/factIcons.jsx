import { Briefcase, Compass, GraduationCap } from 'lucide-react'

/**
 * Iconos asociados a los «hechos» del perfil (Sobre mí).
 * Los datos (`aboutFacts`) referencian estos ids para mantenerlos
 * como texto plano, sin importar componentes desde src/data.
 */
export const factIcons = {
  graduation: GraduationCap,
  briefcase: Briefcase,
  compass: Compass,
}
